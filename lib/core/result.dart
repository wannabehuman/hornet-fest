// core/result.dart — 성공/실패 래퍼 타입.
//
// 여기서 할 일(직접 구현):
//  - Result<T> (성공값 or 실패) — eGovFrame 공식 예제 device-api-app의
//    core 계층 패턴 참고. datasource/repository 반환에 사용.
//  - 나중에 REST 연동 시 네트워크 에러를 이 타입으로 감싸 화면에 전달.
